/**
 * User class, represent users in the server side.
 * Identified by ID field.
 * need to add authentication info
 */
package server;

public class User {
	int ID;
	public User(int ID){
		this.ID = ID;
	}
}
